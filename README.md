# 🚦 UCCIS Real-Time Traffic Intelligence System

## 📌 Overview

This project implements a real-time Urban City Control Intelligence System (UCCIS) that monitors traffic conditions, analyzes congestion, generates optimized signal plans, and triggers alerts using a rule-based engine.

The system integrates video processing, backend APIs, and a dashboard to provide a complete end-to-end traffic monitoring solution.

---

## ⚙️ Features

* Real-time traffic data processing
* Dynamic signal timing generation
* Rule-based alert detection
* FastAPI backend services
* Streamlit dashboard for visualization

---

## 📸 System Execution

### 🔹 Engine Running & Signal Processing


This screenshot shows the real-time execution of the system engine.

* The engine processes live traffic video continuously
* Vehicle movement is analyzed to calculate traffic density
* Signal plans are generated dynamically based on traffic conditions
* The terminal confirms uninterrupted execution and real-time output

This demonstrates that the core processing and signal optimization modules are functioning correctly.

---

### 🔹 Dashboard (Real-Time UI)

This screenshot shows the Streamlit-based dashboard interface.

* Displays live traffic data for multiple zones
* Shows traffic density, violation count, and congestion levels
* Data is updated in real-time from the backend API
* Provides a simple and clear interface for monitoring

This confirms successful integration between frontend and backend systems.

---

### 🔹 API – Zone Traffic

This screenshot shows the response from the `/zone/traffic` API.

* Returns real-time data for a specific zone
* Includes traffic density, violation count, and congestion level
* Identifies high traffic conditions accurately
* Data is returned in structured JSON format

This verifies that the system can provide zone-specific traffic insights.

---

### 🔹 API – Zone State

This screenshot shows the response from the `/zone/state` API.

* Provides traffic data for all monitored zones
* Includes multiple zone entries in a single response
* Shows congestion levels (LOW / HIGH)
* Includes timestamps and trace IDs for tracking

This demonstrates that the system can monitor and manage multiple zones simultaneously.

---

### 🔹 API Documentation (FastAPI)

This screenshot shows the FastAPI Swagger UI.

* Lists all available endpoints:

  * `/zone/traffic`
  * `/zone/state`
  * `/alerts`
* Displays request parameters and response formats
* Allows real-time API testing directly from the browser

This confirms that the APIs are well-structured and properly documented.

---

### 🔹 Live Alerts

This screenshot shows the alerts generated by the rule engine.

* The system continuously evaluates traffic conditions across zones
* Alerts are triggered based on predefined rules

Example:

* `mumbai_zone_1`:

  * Alerts: HIGH_TRAFFIC, HIGH_VIOLATION
  * Actions: INCREASE_SIGNAL_TIME, SEND_TRAFFIC_POLICE

This indicates:

* High vehicle density
* Presence of traffic violations
* Immediate actions suggested by the system

For other zones:

* No alerts are triggered
* Traffic conditions are within normal limits

Key points:

* Alerts are generated in real-time
* Each alert includes recommended actions
* Trace IDs help in tracking alert events

This proves that the rule engine is actively monitoring and responding to traffic conditions.

---

## 🧠 System Architecture

* Vision Engine → Detects vehicles from video input
* Intelligence Engine → Processes traffic data
* Rule Engine → Generates alerts based on conditions
* Signal Engine → Optimizes signal timings
* API Layer → Provides data endpoints
* Dashboard → Displays real-time information

---

## ✅ Conclusion

The system successfully demonstrates:

* Real-time traffic monitoring
* Accurate congestion detection
* Intelligent signal optimization
* Automated alert generation
* API-based data access
* Interactive dashboard visualization

Overall, the project provides a complete and functional simulation of an intelligent traffic control system.

---

## 🚀 How to Run

pip install ultralytics opencv-python streamlit 

pip install fastapi uvicorn

python -m uvicorn api:app --reload

pip install -r requirements.txt

python app.py

streamlit run dashboard.py

---


